# Discord Webhook URL (서버 설정 → 연동 → 웹후크 → 새 웹후크에서 생성)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# YouTube Monitor Web Server Port
YT_WEB_PORT=8011

# 로컬 실행 bind 주소. 기본값은 loopback이며 Docker Compose는 컨테이너 안에서만
# 0.0.0.0으로 덮어쓰고 host에는 127.0.0.1:8088로 publish한다.
YT_WEB_HOST=127.0.0.1

# Firefox 프로필 경로 — 컨테이너가 read-only로 마운트해 yt-dlp cookiesfrombrowser로
# 직접 읽는다. 수동 쿠키 추출 없이, 사용자가 평소 Firefox에서 YouTube 로그인만
# 유지하면 자동으로 쿠키가 갱신된다.
#
# Windows: C:\Users\<user>\AppData\Roaming\Mozilla\Firefox\Profiles\<xxx.default-release>
# Linux:   /home/<user>/.mozilla/firefox/<xxx.default-release>
# macOS:   /Users/<user>/Library/Application Support/Firefox/Profiles/<xxx.default-release>
# profiles.ini의 [Install...] 섹션 Default= 경로가 정답.
FIREFOX_PROFILE_PATH=

# 로컬 실행(uv run python main.py)에서 yt-dlp가 쿠키를 읽어 올 브라우저.
# Docker에서는 위 FIREFOX_PROFILE_PATH 마운트를 쓰므로 이 값은 무시된다.
# 미설정 시 firefox. chrome, edge, brave 등 yt-dlp가 지원하는 이름을 쓸 수 있다.
YT_COOKIE_BROWSER=firefox

# YouTube Data API OAuth (Docker의 yt-web 컨테이너 기준 경로)
YT_YOUTUBE_CLIENT_SECRETS_FILE=/run/secrets/youtube-client.json
YT_YOUTUBE_TOKEN_FILE=/app/data/youtube-oauth/token.json
YT_YOUTUBE_REDIRECT_URI=http://localhost:8088/api/youtube/oauth/callback

# yt-web이 신뢰할 Host 헤더. OAuth가 포함된 개인용 UI이므로 로컬 호스트만 허용한다.
YT_WEB_ALLOWED_HOSTS=localhost,127.0.0.1
